/bin/
*.db
pelfs-stats.json
docs/TODO.md
/pelfs
# scripts/build-pelican-server.sh's defaults, for a run with no arguments.
# CI passes explicit paths, but those are under $GITHUB_WORKSPACE and so
# land here too.
/pelican-src/
/pelican-bin
/fakeorigin
/fakeorigin.exe
/pelfs.exe

# A go.work pointing at a local pelican checkout is a developer convenience,
# never a property of the repository: it silently overrides go.mod for
# everyone who has one, and CI must resolve the pinned module instead.
go.work
go.work.sum
